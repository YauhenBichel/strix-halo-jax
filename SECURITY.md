# Security

This repository contains a diagnostic script and install notes; it runs no service. `jaxcheck.py`
executes only its own fixed Python snippets in subprocesses.

To report a vulnerability, use GitHub's private vulnerability reporting (Security → Report a
vulnerability) rather than a public issue. You will get an answer within a week.
